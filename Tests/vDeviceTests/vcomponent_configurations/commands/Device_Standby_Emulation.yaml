HdmiCec:
  command: cec_message
  description: Inject a directed Standby (CEC opcode 0x36) from the audio system at logical address 5 to the sink at logical address 0 - drives the sink's standby power-state handling
  message:
    user_defined: true
    payload: ["0x50", "0x36"]
